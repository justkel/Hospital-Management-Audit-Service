# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ActorActivityBucket {
  count: Int!
  label: String!
  timestamp: DateTime!
}

enum ActorActivityPeriod {
  LAST_7_DAYS
  LAST_24_HOURS
}

type ActorActivityStats {
  actorId: String!
  buckets: [ActorActivityBucket!]!
  period: ActorActivityPeriod!
  total: Int!
}

enum AuditDateFilter {
  CUSTOM
  THIS_MONTH
  THIS_WEEK
  TODAY
}

enum AuditDistinctField {
  ACTION
  ACTOR_ID
  ENTITY
}

type AuditLog {
  action: String!
  actorDescription: String
  actorId: String
  actorType: String
  appName: String!
  createdAt: DateTime!
  entity: String!
  entityId: String
  id: ID!
  metadata: JSON
  organizationId: String!
  updatedAt: DateTime!
}

input AuditPaginationInput {
  action: String
  actorId: String
  dateFilter: AuditDateFilter
  endDate: DateTime
  entity: String
  limit: Float!
  page: Float!
  startDate: DateTime
}

type AuditPaginationResult {
  items: [AuditLog!]!
  page: Int!
  pageCount: Int!
  total: Int!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

type PeakHour {
  count: Int!
  hour: String!
  hourOfDay: Float!
  percentage: Float!
}

enum PeakHoursPeriod {
  LAST_3_MONTHS
  LAST_7_DAYS
  LAST_30_DAYS
  THIS_MONTH
  THIS_WEEK
  TODAY
}

type PeakHoursStats {
  averagePerHour: Int!
  busiestHour: PeakHour
  offPeakAverage: Int!
  peakAverage: Int!
  peakHours: [PeakHour!]!
  percentPeak: Int!
  period: String!
  totalActivities: Int!
}

type Query {
  auditLogs(pagination: AuditPaginationInput!): AuditPaginationResult!
  getActorActivityStats(period: ActorActivityPeriod!): ActorActivityStats!
  getAuditDistinctValues(field: AuditDistinctField!): [String!]!
  getAuditLogById(id: String!): AuditLog!
  getAuditLogs: [AuditLog!]!
  getOrganizationPeakHours(period: PeakHoursPeriod!): PeakHoursStats!
}